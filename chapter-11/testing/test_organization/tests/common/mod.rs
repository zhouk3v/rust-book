//
// Submodules in Integration Tests
//

// This is the `common` module (using the older naming convention)
// Files under subdirectories of the `tests` directory don't get compiled as seperate crates or have sections in the test output
pub fn setup() {
  // setup code specific to your library's tests would go here
}